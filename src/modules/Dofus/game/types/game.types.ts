export type GameVersion = {
  id: number
  code: string
  name: string
}

export type GameServer = {
  id: number
  name: string
  code: string
}

export type GameServerDto = {
  id: number
  name: string
  code: string
}
export type GameVersionDto = {
  id: number
  name: string
  code: string
}